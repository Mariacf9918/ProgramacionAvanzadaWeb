@model IEnumerable<ProyectoProgra5.UI.Models.TipoOperacione>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdTipoOperacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Porcentaje)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdInstitucion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdTipoOperacion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Porcentaje)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdInstitucionNavigation.IdInstitucion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdTipoOperacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdTipoOperacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdTipoOperacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
