
@model IEnumerable<ProyectoProgra5.UI.Models.Grupo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdGrupo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdGrado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdInstitucion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdGrupo)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdGradoNavigation)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IdInstitucion)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.IdGrupo">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.IdGrupo">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.IdGrupo">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

