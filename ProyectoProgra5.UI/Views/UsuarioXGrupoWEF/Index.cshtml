@model IEnumerable<ProyectoProgra5.UI.Models.UsuarioXGrupo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cedula)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdGrupo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CedulaNavigation.Contrasena)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdGrupoNavigation.IdGrupo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdUsuarioXgrupo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdUsuarioXgrupo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdUsuarioXgrupo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
